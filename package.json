{
  "name": "msia-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "postinstall": "npm --prefix server ci || npm --prefix server install && npm --prefix server run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
