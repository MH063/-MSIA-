{
  "name": "msia-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "_postinstall": "npm --prefix server ci || npm --prefix server install && npm --prefix server run build",
    "test:full-flow-v3": "powershell -ExecutionPolicy Bypass -File ./test-full-flow-v3.ps1"
  },
  "dependencies": {
    "@prisma/client": "^7.3.0",
    "@types/d3": "^7.4.3",
    "d3": "^7.9.0",
    "echarts": "^6.0.0",
    "prisma": "^7.3.0"
  }
}
