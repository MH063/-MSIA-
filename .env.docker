# ============================================
# MSIA Docker 部署环境变量配置
# 医学生智能问诊辅助系统 - Docker 环境专用
# ============================================

# 数据库配置
DB_USER=postgres
DB_PASSWORD=msia_secure_password_2024
DB_NAME=MSIA
DB_PORT=5432

# 服务端口配置
SERVER_PORT=4000
CLIENT_PORT=80

# 后端服务配置
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://127.0.0.1,http://127.0.0.1:80
OPERATOR_TOKEN=msia_docker_token_change_in_production

# 数据库连接池配置
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# 文件上传配置
MAX_FILE_SIZE=10485760

# 缓存配置
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# 日志配置
LOG_LEVEL=info
